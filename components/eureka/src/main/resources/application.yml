spring:
  application:
    name: eureka
  output:
    ansi:
      enabled: always

server:
  port: 8282

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

logging:
  level:
    com:
      netflix:
        eureka: OFF
        discovery: OFF
